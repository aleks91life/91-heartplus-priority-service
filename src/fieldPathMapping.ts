export const fieldPathMapping: { [key: string]: string } = {
    //this is a field for testing purposes
    field: 'otherFields[0].someField'

    // Here there should be similar fields for each type of rule
    // Note: These mappers vary based on the structure of the incoming transmission data
  };

  